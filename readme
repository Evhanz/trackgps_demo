Para instalar es necesario :

Con pip3 - para python 3 
Pip3 install umm
pip3 install pytz
pip3 install requests
#for Mac pip3 install psycopg2-binary
pip3 install psycopg2
pip3 install Faker

Con pip - para python
pip install umm
pip install pytz
pip install requests
#for Mac pip install psycopg2-binary
pip install psycopg2
pip install Faker


Las Query a ejecutar se encuentra en el archivo file.sql